spring.application.name=espol

logging.level.root=INFO

#logging appender consola
logging.level.org.springframework.web=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%highlight(%-5level)] [%cyan(%thread)] %magenta(%logger{15}) - %msg%n

#Especificacion de JPA y hibernate - con MySQL con variables de entorno
spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

###  Credentials de la base de datos
#ejemplo
#spring.datasource.url=jdbc:mysql://localhost:3306/securetxt?createDatabaseIfNotExist=true
#spring.datasource.username=root
#spring.datasource.password=root

# Configuración de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/espol_rsa?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Configuración de archivos multipart
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Configuración del servidor
server.port=8080
server.servlet.context-path=/

# Logging
logging.level.com.espol=DEBUG
logging.level.org.springframework.web=DEBUG
